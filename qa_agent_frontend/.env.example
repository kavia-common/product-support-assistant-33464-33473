# PUBLIC_INTERFACE
# Base URL of the backend API for Q&A service. Do not include trailing slash.
# The orchestrator will copy required variables into .env at deploy time.
REACT_APP_API_BASE_URL=https://your-backend.example.com
